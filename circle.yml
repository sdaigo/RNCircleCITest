machine:
  xcode:
    version: 8.3.3

dependencies:
  override:
    - brew install node

test:
  override:
    - echo 'skip test!'

deployment:
  preview:
    branch: preview-markdown
    commands:
      - fastlane
