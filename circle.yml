machine:
  xcode:
    version: 8.3.3
  node:
    version: "6.9.1"

dependencies:
  override:
    - brew install node

deployment:
  preview:
    branch: preview-markdown
    commands:
      - echo 'hi!'
